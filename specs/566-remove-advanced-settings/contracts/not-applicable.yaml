# Contract: Not Applicable
#
# Feature: 566-remove-advanced-settings
# Date: 2026-04-02
#
# This feature is a pure UI component removal. No API contracts are
# created or modified. The existing backend settings API endpoints
# (GET/PUT /api/settings/global) remain unchanged and are out of scope.
#
# The frontend will simply stop calling these endpoints from the
# SettingsPage component. The endpoints remain available for other
# consumers (e.g., admin tools, other pages).

not_applicable: true
reason: "Pure frontend component removal — no API contract changes"
