# Implementation Plan: 100% Test Coverage with Bug Fixes

**Branch**: `001-full-test-coverage` | **Date**: 2026-03-30 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/001-full-test-coverage/spec.md`

## Summary

Raise both backend (78% → 100% line, 69% → 100% branch) and frontend (50%/44%/41% → 100%) test coverage in 6 phases, fixing bugs and errors discovered along the way. This is a test-coverage and bug-fix-only effort — no feature work or unnecessary refactoring. The approach prioritizes high-value gaps first (untested services, components at 0–30%, branch coverage everywhere), follows existing test conventions exactly, and locks in 100% thresholds in CI at the end.

## Technical Context

**Language/Version**: Python ≥3.12 (backend), TypeScript with Node.js 20 (frontend)
**Primary Dependencies**: FastAPI 0.135+, Pydantic 2.12+, agent-framework-core 1.0.0b1+ (backend); React 18+, Vite, shadcn/ui (frontend)
**Storage**: SQLite via aiosqlite 0.22+ (backend); N/A (frontend)
**Testing**:
- Backend: pytest 9.0+ with pytest-asyncio, pytest-cov 7.0+, mutmut 3.2+, Hypothesis 6.131+, freezegun 1.4+, bandit 1.8+
- Frontend: Vitest 4.0+, @testing-library/react 16.3+, @fast-check/vitest 0.3+, jest-axe 10.0+, Playwright 1.58+, @stryker-mutator/core 9.6+
**Target Platform**: Linux server (backend), browser (frontend)
**Project Type**: Web application (monorepo: `solune/backend/` + `solune/frontend/`)
**Performance Goals**: N/A — this feature is test infrastructure only
**Constraints**: No new test frameworks; follow existing conventions exactly; scope limited to coverage + bug fixes
**Scale/Scope**: ~148 backend source files / 202 test files; ~265 frontend source files / 165 test files

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Specification-First Development | ✅ PASS | spec.md completed with prioritized user stories (P1–P4), Given-When-Then scenarios, clear scope boundaries |
| II. Template-Driven Workflow | ✅ PASS | All artifacts follow canonical templates in `.specify/templates/` |
| III. Agent-Orchestrated Execution | ✅ PASS | Plan generated by speckit.plan agent; tasks will be generated by speckit.tasks agent |
| IV. Test Optionality with Clarity | ✅ PASS | Tests are explicitly the core deliverable of this feature — mandated by the specification itself |
| V. Simplicity and DRY | ✅ PASS | No new frameworks, no premature abstraction. Follows existing patterns. Singleton refactor deferred to Phase 6 only after coverage safety net exists |

**Gate result**: ALL PASS — proceed to Phase 0.

## Project Structure

### Documentation (this feature)

```text
specs/001-full-test-coverage/
├── plan.md              # This file
├── research.md          # Phase 0: research findings
├── data-model.md        # Phase 1: coverage data model
├── quickstart.md        # Phase 1: developer quick-start guide
├── contracts/           # Phase 1: verification contracts
│   ├── backend-coverage-contract.md
│   └── frontend-coverage-contract.md
└── tasks.md             # Phase 2 output (created by /speckit.tasks)
```

### Source Code (repository root)

```text
solune/
├── backend/
│   ├── src/
│   │   ├── api/              # FastAPI route handlers (22 files)
│   │   │   ├── dependencies.py   # [Phase 1 bug fix: verify_project_access]
│   │   │   └── rate_limit.py     # [Phase 1 bug fix: timeout guard]
│   │   ├── services/         # Business logic (30+ files)
│   │   │   ├── agent_middleware.py  # [Phase 2: 0% → 100%]
│   │   │   ├── agent_provider.py   # [Phase 2: 0% → 100%]
│   │   │   ├── collision_resolver.py # [Phase 2: 0% → 100%]
│   │   │   ├── agents/       # Agent orchestration subpackage
│   │   │   └── ...
│   │   ├── models/           # Pydantic data models
│   │   ├── middleware/       # Request/response middleware
│   │   ├── migrations/       # SQL migrations [Phase 3: rollback tests]
│   │   ├── exceptions.py     # [Phase 1 bug fix: McpValidationError]
│   │   └── config.py
│   ├── tests/
│   │   ├── conftest.py       # Shared fixtures (422 lines)
│   │   ├── helpers/
│   │   │   ├── factories.py  # Test data factories (244 lines, 14+ factories)
│   │   │   └── assertions.py # Assertion helpers (74 lines)
│   │   ├── unit/             # Unit tests (152 files)
│   │   ├── integration/      # Integration tests (6+ files)
│   │   ├── property/         # Hypothesis property-based tests
│   │   ├── fuzz/             # Fuzzing tests (3 files)
│   │   ├── chaos/            # Chaos engineering tests (5+ files)
│   │   ├── concurrency/      # Race condition tests (4+ files)
│   │   ├── architecture/     # Import rule validation
│   │   └── performance/      # Performance tests
│   └── pyproject.toml        # Coverage config: fail_under=75, mutmut paths
│
├── frontend/
│   ├── src/
│   │   ├── components/       # React components (100+ files)
│   │   │   ├── agents/       # [Phase 4: 0% → 100%, 9 untested files]
│   │   │   ├── board/        # [Phase 4: 24% → 100%, ~20 untested files]
│   │   │   ├── settings/     # [Phase 4: 24% → 100%, ~12 untested files]
│   │   │   ├── pipeline/     # [Phase 4: 28% → 100%, ~15 untested files]
│   │   │   ├── ui/           # shadcn/ui primitives (tested)
│   │   │   ├── chat/         # Chat interface (13 components)
│   │   │   └── ...
│   │   ├── hooks/            # Custom hooks (60+ files)
│   │   │   └── [Phase 4: 4 untested hooks]
│   │   ├── pages/            # Page components (10+ files)
│   │   │   └── [Phase 4: ActivityPage missing test]
│   │   ├── lib/              # Utility libraries (15+ files)
│   │   ├── services/         # API clients
│   │   ├── context/          # React Context providers
│   │   ├── test/             # Test infrastructure
│   │   │   ├── setup.ts      # Vitest setup (polyfills, mocks)
│   │   │   └── test-utils.tsx # renderWithProviders helper
│   │   ├── App.tsx           # [Phase 5: 0% → 100% routing tests]
│   │   └── ...
│   ├── e2e/                  # Playwright E2E tests
│   ├── vitest.config.ts      # Coverage thresholds: 50/44/41/50
│   ├── stryker.config.mjs    # Mutation testing: hooks + lib only
│   └── package.json
│
└── scripts/
    └── validate-contracts.sh
```

**Structure Decision**: Existing web application monorepo (`solune/backend/` + `solune/frontend/`). No structural changes needed — all work targets existing test directories and configuration files.

## Phase Execution Plan

### Phase 1: Fix Known Bugs & CI Errors (P1)

**Goal**: Green baseline — all existing tests pass reliably.

| Task | File | Action |
|------|------|--------|
| Fix devcontainer CI tag | `.devcontainer/devcontainer.json` | Pin `devcontainers/ci` to valid tag (replace `@v0.3`) |
| Fix silent exception swallowing | `backend/src/api/dependencies.py` | Log or re-raise in `verify_project_access()` |
| Add timeout guard | `backend/src/middleware/rate_limit.py` | Add configurable timeout to `RateLimitKeyMiddleware` session resolution |
| Improve McpValidationError | `backend/src/exceptions.py` | Include field-level errors in exception payload |
| Validate green baseline | CI | Run full backend + frontend suites, confirm zero failures |

**Verification**: `uv run pytest` passes; `npm run test` passes; CI green.

### Phase 2: Backend — Untested Services (78% → ~85%)

**Goal**: Cover all untested service modules; reach ≥85% line, ≥80% branch.

| Task | New Test File | Target Module |
|------|--------------|---------------|
| Test agent middleware | `tests/unit/test_agent_middleware.py` | `src/services/agent_middleware.py` (91 lines) |
| Test agent provider | `tests/unit/test_agent_provider.py` | `src/services/agent_provider.py` (133 lines) |
| Test collision resolver | `tests/unit/test_collision_resolver.py` | `src/services/collision_resolver.py` (163 lines) |
| Test tools/presets | `tests/unit/test_tools_presets.py` | `src/services/tools/presets.py` + `chores/presets/` |
| Fix agents.py coverage | `tests/unit/test_agents.py` (extend) | `_get_service()`, filter/sort, pending/purge/bulk branches |
| Fix activity.py branches | `tests/unit/test_activity.py` (extend) | Lines 50, 54, 64, 69 |

**Conventions** (from existing test infrastructure):
- Fixtures: `conftest.py` (`mock_session`, `mock_db`, `mock_settings`, `mock_github_service`, `client`)
- Factories: `tests/helpers/factories.py` (`make_task()`, `make_chat_message()`, etc.)
- Assertions: `tests/helpers/assertions.py` (`assert_api_success()`, `assert_api_error()`)
- Mocking: `AsyncMock` for async services, `patch` for dependency injection
- Organization: class-based test grouping (e.g., `class TestAgentMiddleware:`)

**Verification**: `pytest --cov-branch` ≥85% line, ≥80% branch; `mutmut` kills ≥80% on new services.

### Phase 3: Backend — Branch Coverage Blitz (85% → ~95%)

**Depends on**: Phase 2

**Goal**: Cover every conditional branch in backend code; reach ≥95% line, ≥90% branch.

| Task | Description |
|------|-------------|
| Audit coverage.json | Identify all files with <90% branch coverage |
| Add branch tests | Error paths, edge cases, None values, empty collections for all API and service files |
| Integration tests | Cache layer, encryption key rotation, MCP store, template files I/O errors |
| Extend Hypothesis tests | All Pydantic models roundtrip, URL parsing edge cases, label extraction |
| Migration tests | Rollback and corruption recovery scenarios in `test_migrations.py` |

**Verification**: ≥95% line, ≥90% branch; `mutmut` ≥85% on `src/services/`.

### Phase 4: Frontend — Component Coverage Sprint (50% → ~80%)

**Parallel with**: Phase 3

**Goal**: Cover all untested components; reach ≥80% all metrics.

| Component Category | Current | Files to Test | Target |
|-------------------|---------|--------------|--------|
| Agents | 0% | AddAgentModal, AgentAvatar, AgentCard, AgentChatFlow, AgentIconCatalog, AgentIconPickerModal, AgentInlineEditor, AgentsPanel, BulkModelUpdateDialog, ToolsEditor | 100% |
| Board | 24% | AddAgentPopover, AgentCardSkeleton, AgentConfigRow, AgentDragOverlay, + ~16 more | 100% |
| Settings | 24% | AIPreferences, AISettingsSection, AdvancedSettings, + ~9 more | 100% |
| Pipeline | 28% | ~15 untested files | 100% |
| Layout, Common, Auth, Tools | varies | Remaining untested components | 100% |
| Pages | partial | ActivityPage.test.tsx (missing) | 100% |
| Hooks | partial | useAdaptivePolling, useBoardProjection, useConfirmation, useUndoRedo | 100% |

**Conventions** (from existing test infrastructure):
- Render: `renderWithProviders()` from `src/test/test-utils.tsx`
- A11y: Every test includes `expectNoA11yViolations()` call
- Queries: `@testing-library/react` (`getByText`, `getByRole`, etc.)
- User events: `@testing-library/user-event`
- Mocking: `vi.mock()` for service/hook dependencies

**Verification**: `npm run test:coverage` ≥80% all metrics; `npm run test:a11y` passes.

### Phase 5: Frontend — Branch & Edge Case Coverage (80% → 100%)

**Depends on**: Phase 4

**Goal**: 100% coverage across all frontend metrics.

| Task | Description |
|------|-------------|
| Audit coverage-final.json | Identify all files with <100% branch coverage |
| State testing | Error, loading, empty states for every component |
| Property tests | `@fast-check/vitest` for complex hooks and transforms |
| Negative paths | API errors (401/403/404/500), invalid form inputs, WebSocket disconnect |
| App routing | Route matching, lazy loading, error boundaries for `App.tsx` (0% → 100%) |
| E2E extensions | ActivityPage spec, agent CRUD negative paths, offline/error recovery |

**Verification**: `npm run test:coverage` at 100%; Stryker ≥80% on hooks+lib; all E2E green.

### Phase 6: Hardening & Threshold Lock-In

**Depends on**: All previous phases

**Goal**: Lock 100% thresholds permanently; prevent regression.

| Task | File | Change |
|------|------|--------|
| Backend threshold | `pyproject.toml` | `fail_under` 75 → 100 |
| Frontend thresholds | `vitest.config.ts` | All metrics to 100/100/100/100 |
| Expand mutmut scope | `pyproject.toml` | `paths_to_mutate` from `src/services/` → all `src/` |
| Expand Stryker scope | `stryker.config.mjs` | Add components and pages to `mutateFiles` |
| Refactor singletons | `github_projects/service.py`, `agents.py` | Module-level singletons → DI pattern |
| CI regression guard | `.github/workflows/ci.yml` | Fail on any coverage decrease |

**Verification**: `pytest --cov-fail-under=100`; `npm run test:coverage` at 100% thresholds; CI green end-to-end.

## Constitution Check — Post-Design Re-evaluation

*Re-evaluated after Phase 1 design completion.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Specification-First Development | ✅ PASS | Plan faithfully implements all user stories and acceptance scenarios from spec.md |
| II. Template-Driven Workflow | ✅ PASS | All artifacts (plan.md, research.md, data-model.md, quickstart.md, contracts/) follow canonical templates |
| III. Agent-Orchestrated Execution | ✅ PASS | Plan generated by speckit.plan; tasks to be generated by speckit.tasks; implementation by speckit.implement |
| IV. Test Optionality with Clarity | ✅ PASS | Tests are the explicit deliverable — mandated by the specification. Test-first approach (Red-Green-Refactor) is inherent to coverage work |
| V. Simplicity and DRY | ✅ PASS | No new frameworks, no premature abstraction. Follows existing patterns. Complexity (singleton refactor) explicitly deferred to Phase 6 with justification |

**Post-design gate result**: ALL PASS — plan is ready for `/speckit.tasks`.

## Complexity Tracking

> No constitution violations detected — this section is intentionally empty.

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| — | — | — |
