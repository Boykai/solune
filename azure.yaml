# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: solune
metadata:
  template: solune-azure-deployment
services:
  backend:
    project: ./solune/backend
    host: containerapp
    docker:
      path: Dockerfile
    resourceName: ca-backend-${AZURE_ENV_NAME}
  frontend:
    project: ./solune/frontend
    host: containerapp
    docker:
      path: Dockerfile
    resourceName: ca-frontend-${AZURE_ENV_NAME}
  signal-api:
    host: containerapp
    image: docker.io/bbernhard/signal-cli-rest-api:0.98
    resourceName: ca-signal-${AZURE_ENV_NAME}
infra:
  provider: bicep
  path: infra
  module: main
