# eslint-plugin-jsx-a11y@6.10.2 declares peer dep eslint "^3-9" but does not
# yet include eslint 10.  The plugin is runtime-compatible; this flag avoids
# ERESOLVE failures in npm ci / npm install until upstream catches up.
legacy-peer-deps=true
