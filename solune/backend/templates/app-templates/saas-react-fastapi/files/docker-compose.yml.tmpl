services:
  backend:
    build: ./backend
    ports:
      - "{{port}}:{{port}}"
    environment:
      - APP_NAME={{app_name}}
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
