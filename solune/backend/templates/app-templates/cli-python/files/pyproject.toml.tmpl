[project]
name = "{{app_name}}"
version = "0.1.0"
description = "{{description}}"
requires-python = ">=3.12"
dependencies = [
    "click>=8.1.0",
]

[project.scripts]
{{app_name}} = "{{python_package_name}}.cli:cli"
